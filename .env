# Ports centralization - only external (host) ports configured here.

# External host ports (map host:container)
# Frontend (host) -> container 80
FRONTEND_HOST_PORT=6600

# Main backend (host) -> container (default inside: 4000)
BACKEND_HOST_PORT=6601

# MongoDB host port (host) -> container 27017
MONGO_HOST_PORT=6602

# Mongo Express (web UI) - avoid conflict with mongo host port
MONGOEXPRESS_HOST_PORT=6605

# Nginx (host) -> container 80
NGINX_HOST_PORT=6603

# Wordme backend external port (host) -> container default (4100)
WORDME_BACKEND_HOST_PORT=6004

# Optional: keep JWT_SECRET commented here for convenience
# JWT_SECRET=change_me
